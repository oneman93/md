# Plan

* You create `Go` button when you look at `source folder` text in table
* The button text will be `Go {folder path}`. For example, if source folder is C:\Works, then button text will be `Go C:\Works`.

